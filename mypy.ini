[mypy]
# lib/passport.py uses `from slug import …` as an intra-lib sibling import
# (slug.py lives in lib/). mypy cannot resolve it without lib/ on the path.
# This per-module override suppresses the pre-existing false-positive.
[mypy-slug]
ignore_missing_imports = True
